name: Licitaciones Entre Ríos

on:
  schedule:
    - cron: '0 11 * * 1-5'      # 08:00 de Argentina (UTC-3), lunes a viernes
  workflow_dispatch:             # y también a mano, con un botón

permissions:
  contents: write                # para guardar el CSV y el estado en el repo

jobs:
  scrapear:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar el lector de imágenes
        # El IAPV publica la fecha de apertura en una FOTO: sin esto sus
        # licitaciones quedan sin fecha (todo lo demás funciona igual).
        run: sudo apt-get update -qq && sudo apt-get install -y -qq tesseract-ocr tesseract-ocr-spa

      - name: Instalar dependencias
        run: pip install --quiet requests beautifulsoup4 lxml openpyxl pypdf playwright pytesseract pillow

      - name: Instalar el navegador
        # Hace falta para dos sitios: Paraná devuelve 403 a los datacenters y
        # ENERSA no manda su certificado intermedio. Un navegador real resuelve ambos.
        run: playwright install --with-deps chromium

      - name: Correr el scraper
        env:
          MAIL_USUARIO:  ${{ secrets.MAIL_USUARIO }}
          MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          MAIL_PARA:     ${{ secrets.MAIL_PARA }}
        run: python -u scraper.py

      - name: Guardar resultados en el repo
        run: |
          git config user.name  "licitaciones-bot"
          git config user.email "bot@users.noreply.github.com"
          git add -f licitaciones.csv estado.json Licitaciones-Entre-Rios.xlsx
          git diff --staged --quiet || git commit -m "Licitaciones al $(date -u +%d/%m/%Y)"
          git push

      - name: Subir el Excel como archivo descargable
        uses: actions/upload-artifact@v4
        with:
          name: licitaciones-excel
          path: Licitaciones-Entre-Rios.xlsx
          retention-days: 30
